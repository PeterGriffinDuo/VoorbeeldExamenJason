@model IEnumerable<League>

<div class="list-group">
    @foreach (var league in Model)
    {
        <a class="list-group-item list-group-item-action" asp-route="ByLeagueRoute" asp-route-id="@league.Id">@league.Name</a>
    }
</div>
