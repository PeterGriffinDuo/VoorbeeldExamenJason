@model IEnumerable<League>

<ul class="list-group mb-4">
    @foreach (var league in Model)
    {
        <li class="list-group-item">
            <a asp-route="ByLeagueRoute" asp-route-id="@league.Id">@league.Name</a>
        </li>
    }
</ul>
