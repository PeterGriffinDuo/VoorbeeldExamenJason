@model HomeIndexVM

<h4>@Model.Title</h4>
@await Component.InvokeAsync("LeagueList")

@if (Model.SelectedLeagueId == null)
{
    <p>Selecteer een League</p>
}
else
{
    <partial name="_GameListPartial" model="Model.Games" />
}
